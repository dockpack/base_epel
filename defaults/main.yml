---

# define or undefine the base_proxy_url, bu do it on the block
#base_proxy_url: http://172.16.1.2:8080
#base_proxy_env:
#  http_proxy: "{{ base_proxy_url | default(omit) }}"
#  https_proxy: "{{ base_proxy_url | default(omit) }}"
